---
const { label, type = 'button', onClick } = Astro.props;
---

<div class='w-full flex justify-end'>
    <button
        type={type}
        onclick={onClick}
        class='bg-slate-50 text-black border border-black font-semibold py-2 px-4 w-full md:w-1/2 rounded-md hover:bg-slate-100 cursor-pointer active:scale-[0.99]'
    >
        {label}
    </button>
</div>
