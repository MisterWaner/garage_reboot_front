---
const { label, name, id, placeholder, required, value } = Astro.props;
---

<div class='flex flex-col mb-4'>
    <label for={id} class='font-montserrat font-semibold mb-1'>{label}</label
    ><textarea
        id={id}
        name={name}
        placeholder={placeholder}
        required={required}
        value={value}
        class='border border-slate-300 rounded-md p-2 w-full'
    ></textarea>
</div>
